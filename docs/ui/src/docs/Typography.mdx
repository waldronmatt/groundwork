import { Meta } from '@storybook/blocks';
import { Unstyled } from '@storybook/blocks';
import { FontFamily, Typography } from '@waldronmatt/story-tokens-ui/lib/index';

<Meta title="Design Tokens/Typography" />

# Typography

<FontFamily token={{ prefix: 'sl', category: 'font-family' }}>
  <Unstyled>
    <h1>The quick brown fox jumped over the lazy dog.</h1>
    <h2>The quick brown fox jumped over the lazy dog.</h2>
    <h3>The quick brown fox jumped over the lazy dog.</h3>
    <h4>The quick brown fox jumped over the lazy dog.</h4>
    <h5>The quick brown fox jumped over the lazy dog.</h5>
    <h6>The quick brown fox jumped over the lazy dog.</h6>
    <p>The quick brown fox jumped over the lazy dog.</p>
  </Unstyled>
</FontFamily>

<Typography token={{ prefix: 'sl', category: 'font-size' }} />

<Typography token={{ prefix: 'sl', category: 'font-weight' }} />

<Typography token={{ prefix: 'sl', category: 'letter-spacing' }} />

<Typography token={{ prefix: 'sl', category: 'line-height' }} />
